# VideoLingoLite Web UI Demo

## 简介

这是 VideoLingoLite 的原生 HTML/CSS/JS Web 界面，采用 Apple 官网设计风格。代码采用模块化架构，方便后续接入后端 API。

## 特性

- ✨ **Apple 设计风格**：完整的 Apple Design System
- 🎨 **纯原生实现**：HTML + CSS + JavaScript，无需框架
- 📦 **模块化架构**：ES6 模块化，代码清晰易维护
- 📱 **响应式设计**：适配桌面和移动端
- ⚡ **轻量高效**：无构建步骤，直接打开即可使用
- 💾 **状态持久化**：使用 localStorage 保存数据
- 🎭 **精美动画**：滚动淡入、模态框等流畅过渡

## 文件结构

```
demo/
├── index.html              # 主页面入口
├── styles/                 # 样式文件
│   ├── base.css           # CSS 变量、基础重置、排版
│   ├── layout.css         # 布局样式（导航栏、容器）
│   └── components.css     # 组件样式（上传、文件列表、模态框）
├── js/                     # JavaScript 模块
│   ├── app.js             # 主应用入口
│   ├── utils.js           # 工具函数
│   ├── state.js           # 状态管理
│   └── components/        # 组件模块
│       ├── modal.js           # 模态框组件
│       ├── confirmDialog.js   # 确认对话框组件
│       ├── fileUpload.js      # 文件上传组件
│       └── fileList.js        # 文件列表组件
└── README.md               # 说明文档
```

## 架构说明

### 模块划分

**核心模块：**
- `utils.js` - 工具函数（ID 生成、HTML 转义、文件操作、Toast 等）
- `state.js` - 状态管理（文件列表、术语、历史、配置等，支持 localStorage 持久化）
- `app.js` - 主应用入口（初始化、事件监听、组件协调）

**组件模块：**
- `modal.js` - 模态框（术语管理、历史记录、设置）
- `confirmDialog.js` - 确认对话框（苹果风格）
- `fileUpload.js` - 文件上传（点击上传、拖拽上传）
- `fileList.js` - 文件列表（展示、操作按钮、徽章更新）

### 样式组织

- `base.css` - CSS 变量系统、基础重置、排版系统、按钮系统
- `layout.css` - 导航栏、容器系统、Hero 区域、底部
- `components.css` - 所有 UI 组件样式（上传、列表、模态框、动画）

### 数据流

```
用户操作 → 组件事件 → 状态更新 → localStorage
                ↓
            UI 重新渲染
```

## 使用方法

### 方法 1：直接打开（推荐）

```bash
# Windows
start demo/index.html

# macOS/Linux
open demo/index.html
```

### 方法 2：本地服务器

由于使用了 ES6 模块（`type="module"`），某些浏览器可能需要通过 HTTP 服务器访问：

```bash
# Python 3
cd demo
python -m http.server 8000

# Node.js (使用 npx)
npx serve demo

# 然后访问
# http://localhost:8000
```

## 功能说明

### 已实现功能

- ✅ Apple 风格导航栏（带徽章提示）
- ✅ Hero 欢迎区域
- ✅ 文件拖拽上传
- ✅ 文件列表展示
- ✅ 文件操作（转录、翻译、导出、删除）
- ✅ 苹果风格确认对话框
- ✅ 模态框（术语、历史、设置）
- ✅ 滚动动画效果
- ✅ Toast 消息提示
- ✅ 本地状态持久化

### 待集成功能

- ⏳ 与后端 API 对接（FastAPI/Flask）
- ⏳ 实际的文件上传处理
- ⏳ 翻译进度显示（WebSocket）
- ⏳ 字幕编辑器
- ⏳ 术语管理 CRUD
- ⏳ 历史记录展示

## 设计系统

### 色彩

```css
--apple-blue: #0071e3;
--apple-orange: #ff9500;
--apple-bg-primary: #ffffff;
--apple-text-primary: #1d1d1f;
```

### 字体

```css
--apple-font-stack: -apple-system, BlinkMacSystemFont, "SF Pro Display",
  "SF Pro Text", "Segoe UI", "Helvetica Neue", Arial, sans-serif;
```

### 组件

- 按钮（主要/次要/幽灵/危险）
- 卡片（常规/大尺寸）
- 导航栏（毛玻璃效果）
- 模态框（淡入缩放动画）
- 确认对话框（苹果风格）
- 徽章（数量提示）

## 浏览器兼容性

- ✅ Chrome/Edge 90+
- ✅ Safari 14+
- ✅ Firefox 88+
- ⚠️ IE 11（不支持，使用了现代 CSS 特性和 ES6 模块）

## 后续开发建议

### 1. 创建 API 模块

```js
// js/api.js
export async function uploadFile(file) {
  const formData = new FormData();
  formData.append('file', file);
  const response = await fetch('/api/upload', {
    method: 'POST',
    body: formData
  });
  return response.json();
}
```

### 2. 添加 WebSocket 进度推送

```js
// js/websocket.js
export function connectWebSocket() {
  const ws = new WebSocket('ws://localhost:8000/ws');
  ws.onmessage = (event) => {
    const data = JSON.parse(event.data);
    updateProgress(data);
  };
}
```

### 3. 集成 VideoLingoLite 后端

- 创建 FastAPI 服务提供 RESTful API
- 实现文件上传接口
- 实现转录/翻译接口
- 使用 WebSocket 推送处理进度

## 参考源

- UI 设计参考：`example/` 目录（React 版本）
- 设计风格：Apple.com 官方网站
